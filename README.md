# Simple servo sweeper

## Dependencies
* node.js && npm [the LTS here installs both](https://nodejs.org/en/)

## Setup
* clone this repo
* `cd simpleServoSweep`
* `npm install`

## Operate
* `npm run sweep`

